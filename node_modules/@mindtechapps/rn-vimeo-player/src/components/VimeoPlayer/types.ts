import { StyleProp, ViewStyle } from 'react-native';

import { LoaderProps } from '../Loader/types';

export interface VimeoPlayerProps extends LoaderProps {
  videoUrl: string;
  backgroundColor?: string;
  style?: StyleProp<ViewStyle>;
  onReady?: () => void;
  onError?: () => void;
}
